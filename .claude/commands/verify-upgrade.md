---
description: Run the upgrade-path skill to verify migration scripts.
---

Invoke the `upgrade-path` skill. Version pair to verify: $ARGUMENTS (if empty, check all version pairs in the .control file).
