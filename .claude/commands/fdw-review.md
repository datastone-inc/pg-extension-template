---
description: Run the fdw-review skill on Foreign Data Wrapper implementations.
---

Invoke the `fdw-review` skill. Scope: $ARGUMENTS (if empty, search the repo for `fdw_handler` / `FdwRoutine` and review every `.c` file that defines an FDW).
