---
description: Run the code-review skill on the current branch (or specified scope).
---

Invoke the `code-review` skill. Scope: $ARGUMENTS (if empty, default to changes since the merge base with main).
