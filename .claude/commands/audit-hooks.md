---
description: Run the pg-hook-audit skill on PostgreSQL hook implementations.
---

Invoke the `pg-hook-audit` skill. Scope: $ARGUMENTS (if empty, search all `.c` files in the repo for hook registrations).
