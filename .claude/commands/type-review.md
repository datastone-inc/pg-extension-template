---
description: Run the pg-type-review skill on custom-type implementations.
---

Invoke the `pg-type-review` skill. Scope: $ARGUMENTS (if empty, search the repo for `CREATE TYPE` and the corresponding in/out/send/recv functions).
